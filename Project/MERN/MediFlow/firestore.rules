rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo Mode: Allow all read and write access so the Seed Demo Data function
    // can successfully clear the old collections and populate the new ones.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
